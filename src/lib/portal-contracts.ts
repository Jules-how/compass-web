// Compatibility adapter for the existing portal imports. The schemas and DTOs
// live in the pure shared package so Compass desktop and Compass-Web consume
// exactly the same validation contract.
export * from '@switchflow/compass-core'
